# Managed by Ansible — Ping Identity Platform environment (G-01, G-14)
export JAVA_HOME={{ java_home }}
export IG_INSTANCE_DIR={{ gateway_instance_dir }}
export PATH="$PATH:{{ ds_home }}/bin:{{ amster_home }}"
